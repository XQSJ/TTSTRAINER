{
  "_comment": "独立 mobile v3 模型：Android 可直接输入普通文本；使用新名称从零训练，公共 WAV 自动复用 / Independent mobile v3 model with direct Android text input; train under a new name while reusing shared WAV data",
  "task": "train",
  "preset": "mobile",
  "experiment": {
    "name": "mobile_tts",
    "languages": ["zh", "en", "ja", "ko", "fr", "es", "pt"],
    "device": "cuda:0"
  },
  "dataset": {
    "sentences_per_language": 2000,
    "speakers": {
      "xiaoling": "voice_xiaoling"
    }
  },
  "training": {
    "batch_size": 4,
    "epochs": 200
  }
}
