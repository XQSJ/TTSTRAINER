{
  "_comment": "高质量训练基线示例；使用新模型名，但可复用相同 voice.id 的共享 WAV / Higher-quality baseline example; use a new model name while reusing shared WAVs with the same voice.id",
  "preset": "quality",
  "experiment": {
    "_comment": "使用新模型名从零训练；不要加载紧凑模型 checkpoint / Use a new name and train from scratch; do not load a compact checkpoint",
    "name": "model_quality_1",
    "languages": ["zh", "en"]
  },
  "text_generation": {
    "_comment": "替换成自己的共享文本设置；相同语义配置会复用公共语料 / Replace with your shared text settings; identical semantic settings reuse the corpus",
    "enabled": true,
    "provider": "builtin",
    "sentences_per_language": 20
  },
  "generation": {
    "_comment": "voice 字段必须与旧配置完全相同才能复用 WAV / Voice fields must exactly match the old config to reuse WAVs",
    "voice": {
      "id": "voice_01",
      "mode": "design",
      "speaker": "voice_01",
      "prompt": "A warm, natural adult voice with clear pronunciation, medium pitch, steady pace, and calm friendly delivery.",
      "reference_text": "Hello, this is the reference voice used to create a consistent multilingual speaker.",
      "reference_language": "en"
    }
  },
  "training": {
    "_comment": "先用 batch_size=4；显存不足改为 2，质量训练不要只跑 20 epoch / Start at batch_size=4; use 2 on OOM and do not stop quality training at 20 epochs",
    "batch_size": 4,
    "epochs": 200
  }
}
